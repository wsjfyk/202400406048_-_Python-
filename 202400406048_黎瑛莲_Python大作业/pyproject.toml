[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "202400406048-liyinglian-python-assignment"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["pandas", "matplotlib", "pillow"]

[tool.setuptools.packages.find]
where = ["src"]
